# Optional Hugging Face settings. Do not put real tokens in committed files.
HF_HOME=.cache/huggingface
HF_TOKEN=

# Evaluation defaults.
PYTHONPATH=src
INPUT_JSONL=outputs/fixtures/math_loophole_eval.jsonl
OUTPUT_DIR=outputs/task3_eval
CHECKPOINT=Qwen/Qwen2.5-1.5B-Instruct
LORA_ADAPTER=
DRY_RUN=1

# Optional sync destination, such as a mounted drive or rclone target.
DEST=
