[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "trace-task3-eval"
version = "0.1.0"
description = "Task 3 evaluation MVP for TRACE + RLFR reward-hacking experiments."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
